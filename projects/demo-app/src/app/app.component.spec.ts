import { AppComponent } from "./app.component"

describe("AppComponent", () => {
  it("should appComponent instance has been defined", () => {
    const appComponent = AppComponent
    expect(appComponent).toBeDefined()
  })
})